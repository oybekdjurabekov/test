export default{
		namespaced:true,
		state:{
			items:[],
		},
		getters:{
		},
		mutations:{
			// onInput(state, payload){
	  //            let data = state.items[payload.index];
	  //            console.log(state.controls);
	  //            // state.controls[payload.index].error = !data.pattern.test(payload.value);
	  //            // state.controls[payload.index].activated = true;
   //          } 
		},
		actions:{

		}
	}